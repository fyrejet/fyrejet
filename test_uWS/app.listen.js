
var express = require('./index.js')

describe('app.listen()', function () {
  it('should wrap with an HTTP server', function (done) {
    console.log('\x1b[31m%s\x1b[0m', 'Test is removed for uWebSockets, Fyrejet doesn\'t conform to Express API here')
    done()
  })
})
